{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["file_path"],
  "properties": {
    "file_path": {
      "type": "string",
      "description": "Path to the CSV, TSV, Parquet, or Excel file to profile"
    },
    "sample_rows": {
      "type": "integer",
      "default": 5,
      "description": "Number of example rows to include per column"
    },
    "max_categories": {
      "type": "integer",
      "default": 20,
      "description": "Maximum number of unique values to list for categorical columns"
    }
  }
}
